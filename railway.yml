name: respir
env: python-3.8
commands:
  - pip install -r requirements.txt
  - python manage.py migrate
  - python manage.py collectstatic --noinput
start: gunicorn respir.wsgi:application --bind 0.0.0.0:5000 --log-level debug --timeout 180

